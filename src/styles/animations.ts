import { keyframes } from 'styled-components'

export const fadeIn = keyframes`
  from {
    opacity: 0
  }

  to {
    opacity: 1
  }
`

export const rotate = keyframes`
  from {
    rotate: 0deg;
  }

  to {
    rotate: 360deg;
  }
`
